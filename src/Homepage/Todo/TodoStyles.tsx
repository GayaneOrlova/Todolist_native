import {StyleSheet} from 'react-native';

const TodoStyles = StyleSheet.create({
  container: {
    flex: 1,
    marginTop: 70,
    alignItems: 'center',
  },
  input: {
    flexDirection: 'row',
    alignItems: 'center',
  },
});

export default TodoStyles;
